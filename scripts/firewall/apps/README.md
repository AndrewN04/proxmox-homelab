# Apps VM Docker port filtering (optional)

Most Apps VM services are bound to the LAN IP (192.168.4.202:port:port), which is already a strong default.
If you also want enforcement via DOCKER-USER, add a similar ruleset here.
